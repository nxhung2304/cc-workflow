# Preferred Libraries

**Auth**: Devise (Rails), Firebase Auth (mobile)
**Admin**: Avo (Rails), AdminLTE (React)
**State**: Redux Toolkit (React), Provider (Flutter)
**Database**: PostgreSQL, ActiveRecord (Rails)

**Ban**: Avoid custom auth, use gems.
